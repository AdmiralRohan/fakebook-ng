import { AutoAdjustTextareaDirective } from './auto-adjust-textarea.directive';

describe('AutoAdjustTextareaDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoAdjustTextareaDirective();
    expect(directive).toBeTruthy();
  });
});
